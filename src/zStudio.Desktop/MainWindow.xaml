<Window x:Class="Recoil.Zbd.Desktop.MainWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:local="clr-namespace:Recoil.Zbd.Desktop"
 Title="zStudio" Icon="/Recoil.Zbd.Studio;component/Assets/studio.ico" Width="1560" Height="940" MinWidth="1080" MinHeight="650"
 Background="{DynamicResource ApplicationBackgroundBrush}" UseLayoutRounding="True" AllowDrop="True" Drop="OnDrop" Closing="OnClosing">
 <Window.Resources>
  <SolidColorBrush x:Key="AssetIconBrush" Color="#3C91D4"/>
  <HierarchicalDataTemplate DataType="{x:Type local:FolderNode}" ItemsSource="{Binding Children}">
   <StackPanel Orientation="Horizontal" ToolTip="{Binding ToolTip}">
    <Grid Width="18" Height="18" Margin="0,0,7,0" VerticalAlignment="Center" IsHitTestVisible="False">
     <Viewbox x:Name="FolderGlyph" Visibility="Collapsed">
      <Canvas Width="20" Height="20">
       <Path Data="M2,5 Q2,3.5 3.5,3.5 H7 L9,5.5 H16.5 Q18,5.5 18,7 V15 Q18,16.5 16.5,16.5 H3.5 Q2,16.5 2,15 Z" Fill="#2C73AC" Stroke="#225780" StrokeThickness="0.8"/>
       <Path x:Name="FolderFront" Data="M2,8 H18 V15 Q18,16.5 16.5,16.5 H3.5 Q2,16.5 2,15 Z" Fill="{StaticResource AssetIconBrush}" Stroke="#2B6794" StrokeThickness="0.8"/>
      </Canvas>
     </Viewbox>
     <TextBlock x:Name="FileGlyph" Text="{Binding FileIcon}" Foreground="{StaticResource AssetIconBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
    <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
   </StackPanel>
   <HierarchicalDataTemplate.Triggers>
    <DataTrigger Binding="{Binding File}" Value="{x:Null}">
     <Setter TargetName="FolderGlyph" Property="Visibility" Value="Visible"/>
     <Setter TargetName="FileGlyph" Property="Visibility" Value="Collapsed"/>
    </DataTrigger>
    <DataTrigger Binding="{Binding IsExpanded,RelativeSource={RelativeSource AncestorType={x:Type TreeViewItem}}}" Value="True">
     <Setter TargetName="FolderFront" Property="Data" Value="M5,8 H18 Q19,8 18.6,9.2 L16.5,15.5 Q16.2,16.5 15,16.5 H2.5 L4,9 Q4.2,8 5,8 Z"/>
    </DataTrigger>
   </HierarchicalDataTemplate.Triggers>
  </HierarchicalDataTemplate>
  <HierarchicalDataTemplate DataType="{x:Type local:InspectorNode}" ItemsSource="{Binding Children}">
   <TextBlock Text="{Binding Label}" ToolTip="{Binding Value}" TextTrimming="CharacterEllipsis"/>
  </HierarchicalDataTemplate>
  <HierarchicalDataTemplate DataType="{x:Type local:SceneTreeItem}" ItemsSource="{Binding Children}"><TextBlock Text="{Binding Label}"/></HierarchicalDataTemplate>
  <Style TargetType="TreeView" BasedOn="{StaticResource {x:Type TreeView}}"><Setter Property="BorderThickness" Value="0"/><Setter Property="VirtualizingStackPanel.IsVirtualizing" Value="True"/><Setter Property="VirtualizingStackPanel.VirtualizationMode" Value="Recycling"/></Style>
 </Window.Resources>
 <DockPanel>
  <Menu DockPanel.Dock="Top">
   <MenuItem Header="_File"><MenuItem Header="_Open folder…" InputGestureText="Ctrl+O" Click="OpenFolderClick"/><MenuItem Header="Open _file…" Click="OpenFileClick"/><MenuItem x:Name="RecentMenu" Header="Recent folders"/><Separator/><MenuItem Header="Export _selected…" InputGestureText="Ctrl+E" Click="ExportSelectedClick"/><MenuItem Header="Export all assets in file…" Click="ExportAllClick"/><MenuItem Header="Export selected as JSON…" Click="ExportJsonClick"/><Separator/><MenuItem Header="Save animation _as…" InputGestureText="Ctrl+S" Click="SaveAnimationClick"/><Separator/><MenuItem Header="_Close tab" InputGestureText="Ctrl+W" Click="CloseCurrentClick"/><MenuItem Header="E_xit" Click="ExitClick"/></MenuItem>
   <MenuItem Header="_Edit"><MenuItem Header="Undo animation edit" InputGestureText="Ctrl+Z" Click="UndoAnimationClick"/><MenuItem Header="Redo animation edit" InputGestureText="Ctrl+Y" Click="RedoAnimationClick"/></MenuItem><MenuItem Header="_View"><MenuItem Header="Properties panel" IsCheckable="True" IsChecked="True" Click="PropertiesClick"/><MenuItem Header="Diagnostics" IsCheckable="True" IsChecked="False" Click="DiagnosticsClick"/><Separator/><MenuItem Header="_Theme"><MenuItem Header="System" Click="ThemeClick"/><MenuItem Header="Light" Click="ThemeClick"/><MenuItem Header="Dark" Click="ThemeClick"/></MenuItem><MenuItem Header="Reset layout" Click="ResetLayoutClick"/></MenuItem>
   <MenuItem Header="_Tools"><MenuItem Header="Validate current file" Click="ValidateClick"/><MenuItem Header="Reload current file" InputGestureText="F5" Click="ReloadClick"/><MenuItem x:Name="CancelOperationItem" Header="Cancel export or validation" InputGestureText="Esc" IsEnabled="False" Click="CancelClick"/></MenuItem>
   <MenuItem Header="_Help"><MenuItem Header="Controls and formats" Click="HelpClick"/><MenuItem Header="About zStudio" Click="AboutClick"/></MenuItem>
  </Menu>
  <Border DockPanel.Dock="Top" BorderThickness="0,0,0,1" BorderBrush="#35808080" Padding="12,8">
   <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="300"/></Grid.ColumnDefinitions>
    <Button Content="Open ZBD folder" Click="OpenFolderClick" Padding="16,8" AutomationProperties.Name="Open ZBD folder"/>
    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="14,0"><TextBlock Text="zStudio" FontWeight="SemiBold" FontSize="13"/><TextBlock Text="{Binding RootPath}" Opacity="0.7" FontSize="12" TextTrimming="CharacterEllipsis" ToolTip="{Binding RootPath}"/></StackPanel>
    <TextBox x:Name="GlobalSearch" Grid.Column="2" Text="{Binding GlobalQuery,UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" ToolTip="Search asset names across the selected root (at least two characters)" AutomationProperties.Name="Search all assets" TextChanged="GlobalSearchChanged"/><TextBlock Grid.Column="2" Margin="10,0" VerticalAlignment="Center" Text="Search all assets…" IsHitTestVisible="False" Opacity="0.5"><TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding Text,ElementName=GlobalSearch}" Value=""><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></TextBlock.Style></TextBlock>
   </Grid>
  </Border>
  <StatusBar DockPanel.Dock="Bottom"><StatusBarItem><TextBlock Text="{Binding Status}" TextTrimming="CharacterEllipsis" MaxWidth="1250"/></StatusBarItem></StatusBar>
  <Expander x:Name="DiagnosticsExpander" DockPanel.Dock="Bottom" Header="Diagnostics" IsExpanded="False" MaxHeight="200">
   <ListBox ItemsSource="{Binding Diagnostics}" FontFamily="Consolas" FontSize="11" Height="150" ScrollViewer.HorizontalScrollBarVisibility="Auto"/>
  </Expander>
  <Grid>
   <Grid.ColumnDefinitions><ColumnDefinition x:Name="FilesColumn" Width="215" MinWidth="130"/><ColumnDefinition Width="5"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
   <TabControl x:Name="NavigationTabs" Margin="5,7,0,0">
    <TabItem Header="Files"><TreeView x:Name="FileTree" ItemsSource="{Binding Folders}" MouseDoubleClick="FileDoubleClick" KeyDown="FileTreeKeyDown"><TreeView.ItemContainerStyle><Style TargetType="TreeViewItem" BasedOn="{StaticResource {x:Type TreeViewItem}}"><Setter Property="IsExpanded" Value="True"/></Style></TreeView.ItemContainerStyle></TreeView></TabItem>
    <TabItem Header="Search"><ListBox x:Name="SearchList" ItemsSource="{Binding SearchResults}" MouseDoubleClick="SearchDoubleClick" ScrollViewer.HorizontalScrollBarVisibility="Disabled"><ListBox.ItemTemplate><DataTemplate><StackPanel Margin="3,5"><TextBlock Text="{Binding Display}" TextTrimming="CharacterEllipsis" FontWeight="SemiBold"/><TextBlock Text="{Binding Location}" FontSize="11" Opacity="0.6"/></StackPanel></DataTemplate></ListBox.ItemTemplate></ListBox></TabItem>
   </TabControl>
   <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="Transparent"/>
   <Grid Grid.Column="2">
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
    <TabControl x:Name="DocumentTabs" ItemsSource="{Binding Documents}" SelectedItem="{Binding SelectedDocument,Mode=TwoWay}" SelectionChanged="DocumentChanged" Height="42" Margin="0,5,4,0">
     <TabControl.ItemTemplate><DataTemplate><StackPanel Orientation="Horizontal"><TextBlock Text="{Binding Title}" VerticalAlignment="Center"/><Button Content="×" Tag="{Binding}" Click="CloseTabClick" Padding="4,0" Margin="10,0,0,0" ToolTip="Close tab"/></StackPanel></DataTemplate></TabControl.ItemTemplate>
     <TabControl.ContentTemplate><DataTemplate><Border Height="0"/></DataTemplate></TabControl.ContentTemplate>
    </TabControl>
    <Grid x:Name="Workspace" Grid.Row="1" Visibility="Collapsed">
     <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
     <Border Background="#20D99A28" Padding="10,5" Visibility="{Binding SelectedDocument.IsStale,Converter={StaticResource BoolVisibility}}"><DockPanel><Button DockPanel.Dock="Right" Content="Reload" Click="ReloadClick"/><TextBlock Text="This file changed on disk. Reload to update its contents." VerticalAlignment="Center"/></DockPanel></Border>
     <Grid Grid.Row="1">
      <Grid.ColumnDefinitions><ColumnDefinition x:Name="AssetsColumn" Width="280" MinWidth="190"/><ColumnDefinition Width="5"/><ColumnDefinition Width="*" MinWidth="250"/><ColumnDefinition x:Name="PropertiesSplitterColumn" Width="5"/><ColumnDefinition x:Name="PropertiesColumn" Width="300" MinWidth="0"/></Grid.ColumnDefinitions>
      <Grid DataContext="{Binding SelectedDocument}" Margin="5,4">
       <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
       <TextBox x:Name="AssetFilter" Text="{Binding Query,UpdateSourceTrigger=PropertyChanged}" Margin="2,4" ToolTip="Filter this file by name or record index" AutomationProperties.Name="Filter current assets"/><TextBlock Margin="12,0" VerticalAlignment="Center" Text="Filter this file…" IsHitTestVisible="False" Opacity="0.5"><TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding Text,ElementName=AssetFilter}" Value=""><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></TextBlock.Style></TextBlock>
       <ComboBox Grid.Row="1" ItemsSource="{Binding Kinds}" SelectedItem="{Binding KindFilter}" Margin="3,5" AutomationProperties.Name="Asset type filter"/>
       <TabControl Grid.Row="2">
        <TabItem Header="Assets">
         <DataGrid x:Name="AssetGrid" ItemsSource="{Binding FilteredAssets}" SelectedItem="{Binding SelectedAsset,Mode=TwoWay}" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Extended" SelectionUnit="FullRow" HeadersVisibility="None" RowHeight="64" CanUserAddRows="False" EnableRowVirtualization="True" EnableColumnVirtualization="True" LoadingRow="AssetLoadingRow" SelectionChanged="AssetSelectionChanged" BorderThickness="0" GridLinesVisibility="None" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
          <DataGrid.ContextMenu><ContextMenu><MenuItem Header="Export selected…" Click="ExportSelectedClick"/><MenuItem Header="Export selected as JSON…" Click="ExportJsonClick"/></ContextMenu></DataGrid.ContextMenu>
          <DataGrid.Columns><DataGridTemplateColumn Width="*"><DataGridTemplateColumn.CellTemplate><DataTemplate>
           <Grid Margin="4"><Grid.ColumnDefinitions><ColumnDefinition Width="50"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <Border Background="#16808080" CornerRadius="4" Width="44" Height="44"><Grid><TextBlock x:Name="AssetPlaceholder" Text="▧" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.4"/><Image Source="{Binding Thumbnail}" Stretch="Uniform"/></Grid></Border>
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="5,0"><TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" ToolTip="{Binding Name}"/><TextBlock Text="{Binding Summary}" FontSize="11" Opacity="0.65" TextTrimming="CharacterEllipsis"/><TextBlock Text="{Binding Identity}" FontSize="10" Opacity="0.45"/></StackPanel>
           </Grid>
           <DataTemplate.Triggers><DataTrigger Binding="{Binding Kind}" Value="Texture"><Setter TargetName="AssetPlaceholder" Property="Visibility" Value="Collapsed"/></DataTrigger></DataTemplate.Triggers>
          </DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn></DataGrid.Columns>
         </DataGrid>
        </TabItem>
        <TabItem Header="Scene"><TreeView ItemsSource="{Binding SceneRoots}" SelectedItemChanged="SceneTreeSelected"/></TabItem>
       </TabControl>
      </Grid>
      <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="Transparent"/>
      <Grid Grid.Column="2" Margin="4">
       <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
       <DockPanel Margin="7,5"><StackPanel DockPanel.Dock="Right" Orientation="Horizontal"><Button Content="Export selected" Click="ExportSelectedClick"/><Button Content="Export all" Click="ExportAllClick"/></StackPanel><StackPanel VerticalAlignment="Center"><TextBlock x:Name="PreviewTitle" Text="Select an asset" FontSize="18" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/><TextBlock x:Name="PreviewSubtitle" Opacity="0.6" FontSize="11" TextTrimming="CharacterEllipsis"/></StackPanel></DockPanel>
       <WrapPanel x:Name="ImageToolbar" Grid.Row="1" Visibility="Collapsed" Margin="4"><Button Content="Fit" Click="FitImageClick"/><Button Content="1:1" Click="ActualImageClick" ToolTip="One texture pixel per screen pixel"/><ComboBox x:Name="ChannelCombo" SelectedIndex="0" SelectionChanged="ImageOptionsChanged" Width="90" VerticalAlignment="Center" Margin="5"><ComboBoxItem Content="RGBA"/><ComboBoxItem Content="RGB"/><ComboBoxItem Content="Alpha"/><ComboBoxItem Content="Red"/><ComboBoxItem Content="Green"/><ComboBoxItem Content="Blue"/></ComboBox><Slider x:Name="ZoomSlider" Minimum="0.05" Maximum="16" Value="1" Width="110" VerticalAlignment="Center" ValueChanged="ZoomChanged"/><Button Content="Palette / header" Click="PaletteClick"/><CheckBox x:Name="SmoothImage" Content="Smooth" VerticalAlignment="Center" Checked="ImageOptionsChanged" Unchecked="ImageOptionsChanged"/></WrapPanel>
       <WrapPanel x:Name="SceneToolbar" Grid.Row="1" Visibility="Collapsed" Margin="4"><Button Content="Frame all" Click="FrameSceneClick"/><CheckBox x:Name="Wireframe" Content="Wireframe" Margin="5" VerticalAlignment="Center" Checked="SceneOptionsChanged" Unchecked="SceneOptionsChanged"/><CheckBox x:Name="TexturesEnabled" Content="Textures" IsChecked="True" Margin="5" VerticalAlignment="Center" Checked="SceneOptionsChanged" Unchecked="SceneOptionsChanged"/><CheckBox x:Name="BoundsEnabled" Content="Bounds" Margin="5" VerticalAlignment="Center" Checked="SceneOptionsChanged" Unchecked="SceneOptionsChanged"/><CheckBox x:Name="FlyEnabled" Content="Fly" ToolTip="Look around from the camera position; wheel moves forward/back" Margin="5" VerticalAlignment="Center" Checked="SceneOptionsChanged" Unchecked="SceneOptionsChanged"/><CheckBox x:Name="BackdropEnabled" Content="Horizon" Margin="5" VerticalAlignment="Center" ToolTip="Move the horizon with the preview camera" Checked="SceneSourceChanged" Unchecked="SceneSourceChanged"/><ComboBox x:Name="WorldDifficulty" Width="104" Margin="3" SelectedItem="{Binding Difficulty, Mode=TwoWay}" ToolTip="Mission difficulty" AutomationProperties.Name="Mission difficulty"/><Button Content="Isolate" Click="IsolateClick"/><Button Content="Show all" Click="ShowAllClick"/><ComboBox x:Name="LodCombo" Width="200" Margin="3" SelectionChanged="SceneSourceChanged" ToolTip="Detail level for each object. Highest detail is the default; objects with fewer variants keep their last available level." AutomationProperties.Name="Scene LOD"/><ComboBox x:Name="TexturePackCombo" Width="145" Margin="5" SelectionChanged="SceneSourceChanged" ToolTip="Texture quality variant"/></WrapPanel>
       <Grid Grid.Row="2" x:Name="PreviewHost">
        <TextBlock x:Name="EmptyPreview" Text="Select an asset to inspect it" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.55"/>
        <ScrollViewer x:Name="ImageScroll" Visibility="Collapsed" Background="{StaticResource Checker}" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" PreviewMouseWheel="ImageWheel">
         <Grid x:Name="ImageSurface" Background="Transparent" MouseDown="ImagePanStart" MouseUp="ImagePanEnd" MouseMove="ImagePanMove" LostMouseCapture="ImagePanCanceled" Unloaded="ImagePanCanceled">
          <Image x:Name="TextureImage" Stretch="Fill" SnapsToDevicePixels="True" HorizontalAlignment="Center" VerticalAlignment="Center" MouseMove="ImagePixelMove"/>
         </Grid>
        </ScrollViewer>
        <ContentControl x:Name="SceneHost" Visibility="Collapsed"/>
        <ContentControl x:Name="AnimationHost" Visibility="Collapsed"/>
        <Grid x:Name="AudioPanel" Visibility="Collapsed" Margin="18"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="180"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions><TextBlock x:Name="AudioDetails" Margin="0,15" TextWrapping="Wrap"/><local:WaveformView x:Name="Waveform" Grid.Row="1"/><Slider x:Name="AudioSeek" Grid.Row="2" Minimum="0" ValueChanged="AudioSeekChanged"/><StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10"><Button Content="Play / pause" Click="PlayAudioClick"/><Button Content="Stop" Click="StopAudioClick"/><TextBlock x:Name="AudioTime" VerticalAlignment="Center" Margin="15,0"/></StackPanel><ListBox x:Name="AudioCues" Grid.Row="4" MaxHeight="220" VerticalAlignment="Top" MouseDoubleClick="CueDoubleClick"/></Grid>
        <TabControl x:Name="StructuredPanel" Visibility="Collapsed"><TabItem Header="Data"><TreeView x:Name="CentralTree"/></TabItem><TabItem Header="Text"><TextBox x:Name="ContentText" IsReadOnly="True" FontFamily="Consolas" FontSize="12" AcceptsReturn="True" TextWrapping="NoWrap" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"/></TabItem><TabItem Header="Sequence events" x:Name="EventsTab" Visibility="Collapsed"><Grid><Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions><DataGrid x:Name="EventGrid" AutoGenerateColumns="False" IsReadOnly="True" SelectionChanged="EventSelected" CanUserAddRows="False"><DataGrid.Columns><DataGridTextColumn Header="Sequence" Binding="{Binding Sequence}" Width="*"/><DataGridTextColumn Header="Event" Binding="{Binding Type}" Width="*"/><DataGridTextColumn Header="Mode" Binding="{Binding Mode}" Width="*"/><DataGridTextColumn Header="Threshold" Binding="{Binding Threshold}" Width="90"/></DataGrid.Columns></DataGrid><TextBlock Grid.Row="1" Margin="8" Text="Thresholds describe engine scheduling; this view does not simulate playback." TextWrapping="Wrap" Opacity="0.65"/></Grid></TabItem></TabControl>
       </Grid>
       <TextBlock x:Name="PreviewInfo" Grid.Row="3" FontSize="11" Opacity="0.65" Margin="8,5" TextWrapping="Wrap" MaxHeight="60" TextTrimming="CharacterEllipsis"/>
      </Grid>
      <GridSplitter Grid.Column="3" Width="5" HorizontalAlignment="Stretch" Background="Transparent"/>
      <TabControl x:Name="InspectorTabs" Grid.Column="4" Margin="0,5,5,0" Background="{DynamicResource TabViewItemHeaderBackgroundSelected}">
       <TabItem Header="Properties">
        <DockPanel>
         <Button DockPanel.Dock="Bottom" Content="Copy properties as JSON" Click="CopyPropertiesClick"/>
         <Grid>
         <ContentControl x:Name="AnimationProperties" Visibility="Collapsed"/>
         <TreeView x:Name="PropertiesTree" Background="Transparent">
          <TreeView.ItemContainerStyle>
           <Style TargetType="TreeViewItem" BasedOn="{StaticResource {x:Type TreeViewItem}}">
            <Style.Triggers><DataTrigger Binding="{Binding Name}" Value="properties"><Setter Property="IsExpanded" Value="True"/></DataTrigger></Style.Triggers>
           </Style>
          </TreeView.ItemContainerStyle>
         </TreeView>
         </Grid>
        </DockPanel>
       </TabItem>
       <TabItem Header="Related"><ListBox x:Name="RelatedList" Background="Transparent" BorderThickness="0" MouseDoubleClick="RelatedDoubleClick"><ListBox.ItemTemplate><DataTemplate><StackPanel Margin="3,5"><TextBlock Text="{Binding Display}" TextWrapping="Wrap"/><TextBlock Text="{Binding Location}" FontSize="11" Opacity="0.6"/></StackPanel></DataTemplate></ListBox.ItemTemplate></ListBox></TabItem>
       <TabItem Header="Bytes"><TextBox x:Name="RawText" Background="Transparent" IsReadOnly="True" FontFamily="Consolas" FontSize="10" AcceptsReturn="True" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" BorderThickness="0"/></TabItem>
      </TabControl>
     </Grid>
    </Grid>
    <StackPanel Grid.Row="1" x:Name="Welcome" VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="580" Margin="30"><TextBlock Text="Explore Recoil’s assets" FontSize="32" FontWeight="SemiBold"/><TextBlock Text="Textures, worlds, models, audio, scripts, and animation sequences — together in one workspace." FontSize="16" Opacity="0.65" Margin="0,16,0,26" TextWrapping="Wrap"/><Button Content="Choose a ZBD folder…" HorizontalAlignment="Left" Click="OpenFolderClick" Padding="22,12"/><TextBlock Text="Open the folder containing image.zbd and the mission directories.&#10;You can also drop a folder or a ZBD file here." Opacity="0.55" Margin="0,24,0,0" TextWrapping="Wrap"/></StackPanel>
   </Grid>
  </Grid>
 </DockPanel>
</Window>
